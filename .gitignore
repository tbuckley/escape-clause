# dependencies
node_modules/

# generated / runtime — ignore claude session state but keep committed project settings
.claude/*
!.claude/settings.json
*.log
*.pid
out.log
err.log

# broker runtime state lives in ~/.clawmini-demo (outside the repo)
