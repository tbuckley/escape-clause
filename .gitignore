# dependencies
node_modules/

# claude session state — this source tree is deliberately NOT a launchable agent
# workspace (the audit enforces no .claude/settings.json or .mcp.json here)
.claude/
.mcp.json
*.log
*.pid

# local agent workspaces and spilled broker outputs
test-workspace/
broker-output-*.txt
